name: Build Android APK
on: workflow_dispatch

concurrency:
  group: android-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest   # вместо ubuntu-20.04
    timeout-minutes: 60
    steps:
      # ... (останалите стъпки без промяна)
